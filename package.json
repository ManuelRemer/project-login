{
  "name": "project-login",
  "version": "1.0.0",
  "description": "a training project on registration and logging built with the MERN toolbox",
  "scripts": {
    "server-start-prod": "NODE_ENV=production node server.js",
    "server-start-dev": "nodemon server.js",
    "client-start-dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install"
  },
  "author": "Manuel Remer",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.9",
    "prettier": "^2.2.1"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.13.2"
  },  
  "engines": {
    "node": "16.x"
  }
}
